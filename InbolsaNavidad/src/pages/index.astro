---
export const prerender = true;

import Base from '../layouts/Base.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import HomeHero from '../components/HomeHero.astro';
import InteractiveHoverButton from '../components/InteractiveHoverButton.astro';
import AnimatedCard from '../components/AnimatedCard.astro';
---

<style>
  /* Animaciones mínimas optimizadas para performance */
  @media (prefers-reduced-motion: no-preference) {
    .animate-ping {
      animation: ping 1.5s cubic-bezier(0, 0, 0.2, 1) infinite;
    }
  }

  @keyframes ping {
    75%, 100% {
      transform: scale(2);
      opacity: 0;
    }
  }
</style>

<Base title="Inbolsa · Home">
  <Header transparent={true} />
  <HomeHero />

  <!-- Sección de contenido descriptivo -->
  <section class="relative py-16 md:py-20 lg:py-28 bg-white overflow-hidden">
    <!-- Fondos decorativos con animación pulse -->
    <div class="hidden md:block absolute top-10 right-0 w-96 h-96 bg-red-50 rounded-full blur-2xl opacity-30 animate-pulse"></div>
    <div class="hidden md:block absolute bottom-10 left-0 w-96 h-96 bg-slate-50 rounded-full blur-2xl opacity-40 animate-pulse" style="animation-delay: 1s;"></div>

    <div class="relative mx-auto max-w-5xl px-4">
      <div class="text-center fade-in-section">
        <span class="inline-flex items-center gap-2 rounded-full bg-gradient-to-r from-red-500/10 to-red-600/10 border border-red-200 px-4 py-2 text-sm font-semibold text-red-700 mb-6">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-500 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-red-600"></span>
          </span>
          Calidad Superior desde 1974
        </span>

        <h2 class="text-2xl md:text-4xl lg:text-5xl font-bold text-slate-900 mb-4 leading-tight">
          Dedicación y excelencia en
          <span class="relative inline-block">
            <span class="relative z-10 bg-gradient-to-r from-red-600 to-red-700 bg-clip-text text-transparent">cada paso</span>
            <span class="absolute bottom-1 left-0 right-0 h-3 bg-red-200 -rotate-1 -z-10"></span>
          </span>
        </h2>

        <div class="max-w-3xl mx-auto space-y-4 md:space-y-6 text-sm md:text-base lg:text-lg text-slate-700 leading-relaxed">
          <p>
            En Inbolsa nos comprometemos a desarrollar productos de alta calidad con 
            <strong class="font-semibold text-slate-900">dedicación y eficacia</strong> para servir al cliente 
            en cada paso de nuestro negocio.
          </p>
          <p>
            Nos enfocamos en la minucia del proceso de producción buscando la 
            <strong class="font-semibold text-slate-900">excelencia en cada paso</strong> desde la elaboración 
            en fábrica hasta la entrega.
          </p>
        </div>

        <div class="mt-10 md:mt-14 grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-8 max-w-4xl mx-auto">
          <div class="text-center p-4 md:p-6 rounded-2xl bg-white border border-slate-100 hover:border-red-200 hover:shadow-lg transition-all duration-200 card-hover">
            <div class="text-2xl md:text-3xl lg:text-4xl font-bold text-red-600 mb-2">50+</div>
            <div class="text-xs md:text-sm text-slate-600 font-medium">Años de experiencia</div>
          </div>

          <div class="text-center p-4 md:p-6 rounded-2xl bg-white border border-slate-100 hover:border-red-200 hover:shadow-lg transition-all duration-200 card-hover">
            <div class="flex flex-col items-center mb-2">
              <div class="flex items-baseline gap-0.5 font-bold text-red-600">
                <span class="text-2xl md:text-3xl lg:text-4xl">52.000</span>
                <span class="text-lg md:text-xl lg:text-2xl">m²</span>
              </div>
            </div>
            <div class="text-xs md:text-sm text-slate-600 font-medium">Área Productiva</div>
          </div>

          <div class="text-center p-4 md:p-6 rounded-2xl bg-white border border-slate-100 hover:border-indigo-200 hover:shadow-lg transition-all duration-200 card-hover">
            <div class="text-2xl md:text-3xl lg:text-4xl font-bold text-indigo-600 mb-2">300+</div>
            <div class="text-xs md:text-sm text-slate-600 font-medium">Empleados</div>
          </div>

          <div class="text-center p-4 md:p-6 rounded-2xl bg-white border border-slate-100 hover:border-slate-300 hover:shadow-lg transition-all duration-200 card-hover">
            <div class="text-2xl md:text-3xl lg:text-4xl font-bold text-slate-600 mb-2">1974</div>
            <div class="text-xs md:text-sm text-slate-600 font-medium">Desde el inicio</div>
          </div>
        </div>

        <div class="mt-12">
          <InteractiveHoverButton 
            href="/valores" 
            variant="primary"
            size="lg"
          >
            Conocer más sobre nosotros
          </InteractiveHoverButton>
        </div>
      </div>
    </div>
  </section>

  <!-- Sección de características -->
  <section class="px-6 py-16 md:py-20 bg-gradient-to-br from-slate-50 to-white relative overflow-hidden">
    <!-- Fondos con animación pulse -->
    <div class="hidden md:block absolute -top-20 -left-20 w-96 h-96 bg-red-50 rounded-full blur-2xl opacity-20 animate-pulse"></div>
    <div class="hidden md:block absolute -bottom-20 -right-20 w-96 h-96 bg-red-50 rounded-full blur-2xl opacity-20 animate-pulse" style="animation-delay: 1s;"></div>

    <div class="relative mx-auto max-w-7xl grid gap-6 md:gap-8 md:grid-cols-3">

      <AnimatedCard variant="gradient" class="group relative overflow-hidden fade-in-section stagger-1">
        <div class="p-8 relative">
          <div class="relative inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-gradient-to-br from-red-500 to-red-600 text-white mb-5 group-hover:scale-105 transition-transform duration-200 shadow-lg">
            <svg class="w-7 h-7" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>

          <h3 class="text-xl font-bold text-slate-900 mb-3 group-hover:text-red-700 transition-colors">
            Valores
          </h3>
          <p class="text-slate-600 mb-6 leading-relaxed">
            Compromiso con la calidad, innovación y servicio al cliente.
          </p>

          <InteractiveHoverButton
            href="/valores"
            variant="ghost"
            size="sm"
            class="w-full"
          >
            Conocer más
          </InteractiveHoverButton>
        </div>

      </AnimatedCard>

      <AnimatedCard variant="gradient" class="group relative overflow-hidden fade-in-section stagger-2">
        <div class="p-8 relative">
          <div class="relative inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-gradient-to-br from-red-500 to-indigo-600 text-white mb-5 group-hover:scale-105 transition-transform duration-200 shadow-lg">
            <svg class="w-7 h-7" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
            </svg>
          </div>

          <h3 class="text-xl font-bold text-slate-900 mb-3 group-hover:text-red-700 transition-colors">
            Industrias
          </h3>
          <p class="text-slate-600 mb-6 leading-relaxed">
            Soluciones especializadas para múltiples sectores industriales.
          </p>

          <InteractiveHoverButton
            href="/industrias"
            variant="ghost"
            size="sm"
            class="w-full"
          >
            Ver industrias
          </InteractiveHoverButton>
        </div>

      </AnimatedCard>

      <AnimatedCard variant="gradient" class="group relative overflow-hidden fade-in-section stagger-3">
        <div class="p-8 relative">
          <div class="relative inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-gradient-to-br from-indigo-500 to-purple-600 text-white mb-5 group-hover:scale-105 transition-transform duration-200 shadow-lg">
            <svg class="w-7 h-7" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>

          <h3 class="text-xl font-bold text-slate-900 mb-3 group-hover:text-indigo-700 transition-colors">
            Historia
          </h3>
          <p class="text-slate-600 mb-6 leading-relaxed">
            Desde 1974, tejiendo calidad superior en polipropileno.
          </p>

          <InteractiveHoverButton
            href="/historia"
            variant="ghost"
            size="sm"
            class="w-full"
          >
            Nuestra trayectoria
          </InteractiveHoverButton>
        </div>

      </AnimatedCard>
    </div>
  </section>

  <Footer />
</Base>
